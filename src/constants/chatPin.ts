/** Đồng bộ backend + web `MAX_PINNED_CHATS_TO_TOP`. */
export const MAX_PINNED_CHATS_TO_TOP = 5;

/** Đồng bộ backend `MAX_PINNED_MESSAGES_PER_CONVERSATION` + web `MAX_PINNED_PER_CONVERSATION`. */
export const MAX_PINNED_PER_CONVERSATION = 5;
