/** Android res/raw resource name (no extension) for incoming call notifications. */
export const CALL_NOTIFICATION_SOUND = "amthanhnhan";

/** Expo / FCM notification channel id for calls. */
export const CALLS_NOTIFICATION_CHANNEL_ID = "calls";
